import config from './validation.json';

export const MAX_TITLE_LENGTH = config.MAX_TITLE_LENGTH;
export const MAX_SLUG_LENGTH = config.MAX_SLUG_LENGTH;
export const MAX_DESCRIPTION_LENGTH = config.MAX_DESCRIPTION_LENGTH;
